namespace ClassLibrary.Entities.Player {
    public enum PlayerAnimationsEnum {
        Idle,
        Move,
        GetDamage,
        Attack,
        Explosion,
        Teleport,
        Converting
    }
}