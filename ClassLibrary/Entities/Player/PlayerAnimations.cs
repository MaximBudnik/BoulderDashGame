namespace ClassLibrary.Entities.Player {
    public enum PlayerAnimations {
        Idle,
        Move,
        GetDamage,
        Attack,
        Explosion,
        Teleport,
        Converting
    }
}